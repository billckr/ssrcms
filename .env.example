# Synaptic Signals — environment configuration
# Copy to .env and fill in values before running.
#
# Alternatively, copy synaptic.toml.example to synaptic.toml for file-based
# configuration. Environment variables (including this file) always override
# values from synaptic.toml. Set CONFIG_FILE=/path to use a custom file path.

# Required: PostgreSQL connection URL
DATABASE_URL=postgres://synaptic:password@localhost:5432/synaptic_signals

# Optional: override bind address (default: 0.0.0.0:3000)
# HOST=0.0.0.0
# PORT=3000

# Required in production: 64+ byte random secret for cookie signing
# Generate with: openssl rand -hex 32
# SECRET_KEY=change-me-in-production

# Optional: log level (default: info)
# LOG_LEVEL=debug

# Optional: log format — "text" (human-readable, default) or "json" (structured, for Datadog/Loki/CloudWatch etc.)
# LOG_FORMAT=json

# Optional: bearer token to protect GET /metrics (leave unset to rely on network-level restriction)
# Generate with: openssl rand -hex 32
# METRICS_TOKEN=your-secret-token-here

# Optional: enable hot reload of templates/plugins (default: false)
# DEV_MODE=true

# Optional: paths (defaults shown)
# THEMES_DIR=themes
# PLUGINS_DIR=plugins
# UPLOADS_DIR=uploads

# Optional: periodically check GitHub Releases for a newer version and show
# an "update available" notice on the admin dashboard (default: true)
# UPDATE_CHECK_ENABLED=false

# Optional: allow a super-admin to trigger the in-app self-updater from
# /admin/whats-new (downloads, checksum-verifies, and applies the latest
# release, then restarts) (default: true)
# SELF_UPDATE_ENABLED=false
